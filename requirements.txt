fastapi==0.95.2
uvicorn==0.30.6
jinja2==3.1.4
pyyaml==6.0.2
pymongo==4.6.2
# NOTE: speedtest-cli is unmaintained (upstream repo is archived; 2.1.3 is the
# final release). It is still available on PyPI and verified installable, but
# do not expect security fixes or Python-version updates from it.
speedtest-cli==2.1.3
